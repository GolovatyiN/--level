# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env (contains Supabase keys)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
tsconfig.app.tsbuildinfo
tsconfig.node.tsbuildinfo
*.tsbuildinfo

# Supabase CLI — local-only state. The link metadata under .temp/ contains
# the project ref (public) but never the access token; the token lives in
# ~/.supabase/access-token, outside the repo.
supabase/.temp/
supabase/.branches/
